/*
 * Give the declaration for two variables called feet and inches.
 *
 * Both variables are of type int and both are to be initialized to
 *  zero in the declaration.
 *
 * Give both initialization alternatives.
 */
#include <iostream>
using namespace std;

int main() {
    int feet = 0;
    int inches(0);

    cout << "Feet:   " << feet << endl;
    cout << "Inches: " << inches << endl;

    return 0;
}
